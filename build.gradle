apply plugin: 'java'
sourceCompatibility = 1.8

apply plugin: 'eclipse'

group = 'com.CE4062.Group22'
version = '0.1'

eclipse {
    classpath {
        downloadSources=true
        containers = [ 'org.eclipse.jdt.launching.JRE_CONTAINER/org.eclipse.jdt.internal.debug.ui.launcher.StandardVMType/JavaSE-1.8' ]
    }
}

apply plugin: 'maven'
buildscript {
    repositories {
        maven {
            url "https://plugins.gradle.org/m2/"
        }
    }
    dependencies {
        classpath 'org.owasp:dependency-check-gradle:3.3.4'
        classpath "gradle.plugin.net.ossindex:ossindex-gradle-plugin:0.4.5-beta"
    }
}

repositories {
    mavenCentral()
}

configurations {
    compile
}

configurations.all {
    transitive = true
}

compileJava {
    options.fork = true
}

dependencies {
    compile group: 'org.apache.struts', name: 'struts2-core', version: '2.1.8'
}

defaultTasks 'build'



